### Lista de Comandos ###

CREATE DATABASE <NOME DO BANCO>; -- COMANDO PARA CRIAÇÃO DO BANCO DE DADOS

USE <NOME DO BANCO>; -- COMANDO PARA ACESSAR O BANCO ESPECIFICO

CREATE TABLE <NOME DA TABELA> ( 
       
       ### CAMPOS DA TABELA ###

);

SHOW TABLES; -- MOSTRA AS TABELAS CRIADAS NO BANCO

DESC <NOME DA TABELA>; -- VERIFICA COMO É A TABELA


SELECT <NOME DA COLUNA> AS <SE VC QUISER INDENTIFICAR A COLUNA> FROM DA TABELA QUE VC QUISER

SELECT <NOME DA COLUNA> AS <SE VC QUISER INDENTIFICAR A COLUNA> FROM <DA TABELA QUE VC QUISER>
WHERE <ELEMENTO> = '<OQUE TEM Q TER COMO FILTRO>'

$$$ FILTRA O ELEMENTO QUE CONTEM O TERMO DENTRO DE UMA FRASE
SELECT <NOME DA COLUNA> AS <SE VC QUISER INDENTIFICAR A COLUNA> FROM <DA TABELA QUE VC QUISER>
WHERE <ELEMENTO> LIKE '<%OQUE TEM Q TER COMO FILTRO%>'


